import React from 'react';
import HomeReadFilters from './HomeReadFilters';

export const HomeReadHeader = () => (
  <div className="content-container">
    <HomeReadFilters />
  </div>
);

export default HomeReadHeader;
